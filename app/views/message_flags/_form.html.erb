<%= form_for(@message_flag) do |f| %>
  <% if @message_flag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@message_flag.errors.count, "error") %> prohibited this message_flag from being saved:</h2>

      <ul>
      <% @message_flag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :message_id %><br>
    <%= f.number_field :message_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
